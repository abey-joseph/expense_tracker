import 'package:flutter/material.dart';

class UserDataEditScreen extends StatelessWidget {
  const UserDataEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("data"),
      ),
    );
  }
}
